<%= simple_form_for Newsletter.new do |f| %>
    <%= f.input :email, as: :email, label: false, wrapper: false, placeholder: "Entrer votre addresse email" %>
    <%= f.submit "S'inscrire", class: "btn btn-primary btn-block" %>
<% end %>
