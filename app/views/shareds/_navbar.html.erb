<nav class="navbar navbar-expand-md navbar-light bg-white sticky-top" style="box-shadow: 0 10px 20px rgba(0,0,0,.1);">
	<!-- " style="box-shadow: 0 2px 10px rgba(0,0,0,.3); -->
	<!-- style="box-shadow: rgba(0, 0, 0, 0.4) 0px 2px 4px 0px, rgba(0, 0, 0, 0.2) 0px 2px 20px 0px; -->
	<!-- box-shadow: 0 10px 32px 0 rgba(0,0,0,.1);
box-shadow: 0 0 40px 0 rgba(0,0,0,0.1);
box-shadow: 0 0.25rem 0.75rem rgba(0, 0, 0, .05);
} -->
<!-- a utiliser pour card   rgba(121,121,121,.35) 1px 10px 20px

box-shadow: 0 2px 10px rgba(0,0,0,.3);

box-shadow: 0 0 40px 0 rgba(0,0,0,0.1)

-->
	<div class="container">
		<%= link_to accueil_path, class: "navbar-brand font-weight-bold text-uppercase" do %>
			<i class="fas fa-home fa-lg"></i> Sico
      <!-- <%= image_tag "", height: 50 %> -->
		<% end %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

      <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav ml-auto d-flex align-items-center">
          <li class="nav-item active mx-1">
            <a class="nav-link" href="<%= accueil_path %>">Accueil <span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item mx-1">
            <a class="nav-link" href="#">Tout les types de bien</a>
          </li>
          <!-- <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Type de bien</a>
            <div class="dropdown-menu" aria-labelledby="dropdown01">
              <a class="dropdown-item" href="#">Action</a>
              <a class="dropdown-item" href="#">Another action</a>
              <a class="dropdown-item" href="#">Something else here</a>
            </div>
          </li> -->

          <li class="nav-item mx-1">
              <%= link_to new_property_path, class: "btn btn-primary" do %>
                <i class="fas fa-bullhorn"></i> Publiez une annonce
                <% end %>
          </li>
          <% if user_signed_in? %>
                <li class="nav-item dropdown mx-1">
                  <!-- <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_user.first_name + " " + current_user.last_name %></a> -->
                    <%= image_tag current_user.profile_photo.url(:thumb), class: "nav-link dropdown-toggle rounded-circle img-fluid profile_photo", id: "navbarDropdown", role: "button", "data-toggle" => "dropdown", "aria-haspopup" => "true", "aria-expanded" => "false" %>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <h6 class="dropdown-header sr-only">
                          <i class="fas fa-user"></i> <%= current_user.first_name + " " + current_user.last_name %>
                        </h6>
                        <%= link_to accueil_path, class: "dropdown-item" do %>
                            <i class="fas fa-list"></i> Mes annonces
                        <% end %>
                        <%= link_to accueil_path, class: "dropdown-item" do %>
                            <i class="fas fa-user"></i> Mon profile
                        <% end %>
                        <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                            <i class="fas fa-cog"></i> Mon compte
                        <% end %>
                        <%= link_to destroy_user_session_path, method: :delete, class: "dropdown-item" do %>
                            <i class="fas fa-sign-out-alt"></i> Déconnexion
                        <% end %>
                    </div>
                </li>
            <% else %>
              <li class="nav-item mx-1">
                <%= link_to new_user_session_path, class: " nav-link" do %>
                    <i class="fas fa-sign-in-alt"></i> Connexion
                <% end %>
              </li>
            <% end %>
        </ul>

      </div>
	</div>
</nav>


<% content_for :dropdown_truc do %>
    <script type="text/javascript">
      $(function(){

           // jQuery methods go here...

           $('.dropdown-toggle').dropdown()

        });
    </script>
<% end %>
